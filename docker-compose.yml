services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: euro24_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./data/schema:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U your_user -d euro24_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  ingestion:
    build: .
    profiles: ["ingest"]
    depends_on:
      postgres:
        condition: service_healthy
    env_file:
      - .env
    environment:
      DATA_SOURCE: live
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: euro24_db
      DB_USER: postgres
      DB_PASSWORD: postgres

volumes:
  pgdata: